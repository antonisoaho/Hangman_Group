// HangMan game
Canvas där man bygger spelet i HTML CSS
Containers som ligger som hidden som poppar upp vid behov //(Restartbox, Category box, Playerbox)

arrays med ord om singleplayer, olika kategorier

// Funktioner
wordGenerator() // genererar ett nytt ord från vald kategori och gör ordet som visas till "_ _ _"
letterCheck() // Kontrollerar om bokstaven finns i ordet för att sedan skriva ut det på vår HTML

//